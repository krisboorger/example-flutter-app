import 'package:flutter/material.dart';
import 'image_switcher_state.dart'; // Import ImageSwitcherState

class ImageSwitcher extends StatefulWidget {
  const ImageSwitcher({Key? key}) : super(key: key);

  @override
  ImageSwitcherState createState() => ImageSwitcherState();
  
  static const routeName = '/navigate';
}
